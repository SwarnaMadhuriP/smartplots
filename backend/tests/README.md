Tests will be added here later.

Suggested layout:

- `unit/` for deterministic helper and service tests
- `integration/` for API and database-backed tests
- `eval/` for agent behavior evaluation cases
